This folder is reserved for shadcn/ui components.

Current foundation is initialized through:
- `components.json`
- Tailwind CSS variables in `src/styles/globals.css`
- shared `cn()` utility in `src/lib/cn.ts`

Add generated shadcn/ui components here as the frontend grows.
